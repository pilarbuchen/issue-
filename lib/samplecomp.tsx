import * as React from 'react';

export default class SampleComp extends React.Component {
    render() {
        return <div>
            This is a sample component that Codux refuses to edit because it think's its an external component.
        </div>
    }
}
